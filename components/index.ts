//  we're going to use this file as a single file that's going to export all of our components so we can import them in a simpler manner
import Hero from './Hero'
import Navbar from './Navbar'
import Footer from './Footer'
import CustomButton from './CustomButton'
export {
    Hero,
    CustomButton,
    Navbar,
    Footer
}